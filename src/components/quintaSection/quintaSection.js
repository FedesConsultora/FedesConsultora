import React from 'react';
import PrensaHeader from './PrensaHeader';
import NotasCarousel from './NotasCarousel';
import AccionFooter from './AccionFooter';

const QuintaSection = () => {
  return (
    <section className="quintaSection">
      <PrensaHeader />
      <NotasCarousel />
      <AccionFooter />
    </section>
  );
};

export default QuintaSection;
